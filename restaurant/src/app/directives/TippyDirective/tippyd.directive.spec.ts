import { TippydDirective } from './tippyd.directive';

describe('TippydDirective', () => {
  it('should create an instance', () => {
    const directive = new TippydDirective();
    expect(directive).toBeTruthy();
  });
});
